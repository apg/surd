(def > (lam (x y)
   (if (= x y)
       '()
       (if (< x y)
           '()
           'true))))

(def fact (lam (x)
   (if (> x 1)
      (* (fact (- x 1)) x)
      1)))

(cons (fact 1)
      (cons (fact 2)
            (cons (fact 5)
                  (cons (fact 10) '()))))

